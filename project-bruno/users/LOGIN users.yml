info:
  name: LOGIN users
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5000/api/v1/users/auth
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
