info:
  name: LOGOUT users
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5000/api/v1/auth
  body:
    type: json
    data: |-
      {
        "email":"idris3367@gmail.com",
        "password":"yuji"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
