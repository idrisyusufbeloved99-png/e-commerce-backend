info:
  name: CREATE coupon
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/v1/coupons
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
